define(function() {
    'use strict';

    var React = require('react');

    var MenuItem = React.createClass({
        render:function() {
            return (
                <div>
                    <span>Salmon Fly</span>
                </div>
            );
        }
    });

    return MenuItem;
});