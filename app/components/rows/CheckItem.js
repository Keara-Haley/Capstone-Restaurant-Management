define(function() {
    'use strict';

    var React = require('react');

    var CheckItem = React.createClass({
        render:function() {
            return (
                <div>
                    <span>Salmon Fly</span>
                </div>
            );
        }
    });

    return CheckItem;
});