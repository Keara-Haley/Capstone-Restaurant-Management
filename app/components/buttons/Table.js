define(function() {
    'use strict';
    var React = require('react');

    var Table = React.createClass({
        render: function () {
            return (
                <div>
                    Table
                </div>
            );
        }
    });
    return Table;
});