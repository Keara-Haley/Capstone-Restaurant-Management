define(function() {
    'use strict';

    var React = require('react');

    var MenuFoodButton = React.createClass({
        render:function() {
            return (
                <button>Food</button>
            );
        }
    });

    return MenuFoodButton;
});