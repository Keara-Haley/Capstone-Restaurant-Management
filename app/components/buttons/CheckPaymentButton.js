define(function() {
    'use strict';

    var React = require('react');

    var CheckPaymentButton = React.createClass({
        render:function() {
            return (
                <button>Payment Processing</button>
            );
        }
    });

    return CheckPaymentButton;
});