define(function() {
    'use strict';

    var React = require('react');

    var CheckButton = React.createClass({
        render:function() {
            return (
                <button>Check</button>
            );
        }
    });

    return CheckButton;
});